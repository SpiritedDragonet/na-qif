请生成一张用于 NeurIPS 风格论文的高质量学术矢量插图。画面主题是“可扩展量子计算的系统压力”，用于论文第一章引入模块化量子计算架构的动机。整体风格应接近精心绘制的 TikZ 或专业矢量图，白色背景，细线条，低饱和配色，文字清晰，布局严谨，适合放入双栏论文或博士论文正文。

画布采用横向宽幅构图，比例约为 2.2:1，例如 2200 px × 1000 px。四周保留充足白边，所有元素对齐到隐含网格。图像主体是一个星状或放射状系统图。画面中心放置一个圆角矩形或柔和圆形中心节点，四个方向分别放置四个压力来源模块：左上、右上、左下、右下。每个压力模块都由一个主标题、一行技术关键词、一句解释性短句和一个小型技术图标组成。四个模块通过短箭头或锥形连接带指向中心节点，表示四类工程压力共同汇聚成“规模化运行压力”。中心节点再通过一条向下的箭头连接到底部的体系结构结论框。

中心节点文字必须写：
规模化运行压力

中心节点使用极浅灰色填充，深灰色描边，圆角较小，线宽约 1.2 pt。中心节点内部文字使用黑色或近黑色，字号明显高于周围说明文字。中心节点周围可以有一圈很淡的环形轮廓或四个短连接端口，使它看起来像系统工程中的汇聚节点。

左上模块表示“物理比特扩容”。该模块主标题必须写：
物理比特扩容

左上模块关键词行必须写：
控制扇出 · 串扰矩阵

左上模块解释句必须写：
寻址、读出与耦合通道进入同一误差预算

左上模块图标应画成一个小型量子处理阵列。用 5×4 或 6×4 的圆点阵列表示物理比特，用淡蓝色小圆点和深蓝色描边。阵列左侧和上方画多条细控制线，控制线扇出到多个比特位点。相邻比特之间画几条浅灰或浅红的弧线，表示串扰耦合。旁边可以放一个微型矩阵热图，矩阵只需 4×4 或 5×5，少量格子用浅红色或橙色强调，表示串扰矩阵中的非零项。该模块的视觉含义是：当物理比特数增长时，控制线、读出通道、耦合通道和串扰项同时增长，并共同占用误差预算。模块填充使用很浅的蓝色，边框使用低饱和蓝色。

右上模块表示“并行读出反馈”。该模块主标题必须写：
并行读出反馈

右上模块关键词行必须写：
综合征吞吐 · 译码延迟

右上模块解释句必须写：
纠错轮次受读出带宽、经典处理和反馈时钟共同限制

右上模块图标应画成多路测量信号进入经典处理单元再返回量子处理区的结构。左侧画 4 到 6 条并行波形线或脉冲线，表示同时读出的 syndrome 数据。中间画一个小芯片或解码器方块，内部写短标签：
decoder

从小芯片右侧画一条短箭头返回到一个小型量子阵列或控制端口，表示反馈。可以在芯片下方画一个极简时钟图标或延迟条，标注：
latency

也可以在波形线上方放一个小标签：
syndrome stream

该模块的视觉含义是：纠错循环的速度由读出吞吐、经典译码和反馈延迟共同决定。模块填充使用很浅的青绿色，边框使用低饱和青绿色。

左下模块表示“长时纠错运行”。该模块主标题必须写：
长时纠错运行

左下模块关键词行必须写：
逻辑寿命预算 · 相关噪声

左下模块解释句必须写：
有效电路深度由每轮逻辑错误率和相关误差尾部决定

左下模块图标应画成沿时间轴重复运行的纠错周期。横向画一条时间轴，时间轴上放 5 到 7 个等距小周期块，每个周期块里可以有一个小勾形 syndrome 标记或短竖线。时间轴上方画一条缓慢下降的平滑曲线，表示逻辑态保真度或生存概率随时间衰减。曲线尾部可以有一小段阴影或加粗区域，表示相关噪声尾部。图标旁边可以用小字标：
logical survival

时间轴下方可以写极短标签：
cycles

该模块的视觉含义是：长时运行的关键量是逻辑寿命、每轮错误率、相关误差和可承受电路深度。模块填充使用很浅的紫灰色，边框使用低饱和紫色。

右下模块表示“标定闭环维护”。该模块主标题必须写：
标定闭环维护

右下模块关键词行必须写：
漂移追踪 · 重标定周期

右下模块解释句必须写：
频率、相位、功率、耦合点需要持续回写到控制模型

右下模块图标应画成参数漂移与反馈校正闭环。左侧画一条缓慢漂移的曲线，曲线从目标虚线附近偏离。右侧画一个控制模型方块，方块里可以放三条短滑杆或旋钮，表示频率、相位和功率参数。曲线和控制模型之间画一个环形箭头，表示测量、估计、更新、回写。曲线旁边可以写短标签：
drift

控制模型方块内可以写短标签：
control model

该模块的视觉含义是：大规模系统需要持续追踪参数漂移，并把重标定结果回写到控制模型。模块填充使用很浅的琥珀色，边框使用低饱和橙色。

底部结论框放在中心节点正下方，宽度略大于中心节点，高度适中。结论框文字必须写：
模块化处理节点 + 可宣告光子互连

结论框填充使用浅绿色或浅蓝绿色，边框比其他模块稍强。结论框内部或上方画一个极简图标：两个小型量子处理节点通过一条波浪形光子链路连接，链路中间上方放一个小圆点或小勾号，表示 heralded success。两个处理节点可以画成小方块阵列，光子链路画成橙色或金色波浪线。中心节点到结论框之间使用单条向下箭头，箭头含义是：系统压力推动体系结构转向模块化处理节点和可宣告光子互连。

整体阅读顺序应清晰。读者先看到中心的“规模化运行压力”，再看到四个方向的压力来源，最后看到底部的体系结构出口。四个模块的主标题是第一阅读层级，关键词行是第二阅读层级，解释句和图标细节是第三阅读层级。文字和图标应保持充足间距，解释句可以自动换成两行，但每行长度应接近，避免文字碰到边框。所有中文文字应使用清晰的无衬线字体，例如思源黑体、Noto Sans CJK 或类似字体。字号层级建议为：中心标题最大，模块标题次之，关键词行中等，解释句最小但仍可读。

图中线条使用深灰色。箭头线宽约 1.2 到 1.6 pt，模块边框线宽约 1.0 到 1.2 pt，图标线宽约 0.8 到 1.0 pt。色彩采用低饱和学术配色：蓝色用于物理比特扩容，青绿色用于并行读出反馈，紫灰色用于长时纠错运行，琥珀色用于标定闭环维护，蓝绿色用于底部模块化架构结论。背景保持纯白。可以使用非常轻微的阴影或完全平面化矢量风格，整体应显得克制、严谨、现代。

图像中所有箭头都应表达明确逻辑关系。四个压力模块的箭头指向中心节点，表示压力汇聚。中心节点的箭头指向底部结论框，表示体系结构响应。图标内部的小箭头也要符合物理或工程含义：控制线指向比特阵列，测量流指向 decoder，decoder 反馈回控制端口，纠错周期沿时间轴推进，标定闭环从漂移观测回到控制模型。

请生成干净、完整、可读的论文插图。画面应像正式论文中的解释性 schematic。所有元素应有足够留白，模块之间保持视觉平衡，四个象限大小接近，中心节点位于准确中心，底部结论框与中心节点垂直对齐。全图保持纯矢量学术图元，采用明亮背景、轻量线条、清晰文字和严谨结构。最终图像应适合后续人工微调文字，并能作为 TikZ 重绘时的精确构图参考。
