请生成一张用于 NeurIPS 风格论文的高质量学术矢量插图。画面主题是“模块化量子处理网络的资源架构”，用于论文第一章第二张图。图像需要解释模块化量子计算从多个 QPU 节点、节点内部角色分工，到远程 Bell 对资源调用链的完整关系。整体应像精心绘制的 TikZ 或专业矢量 schematic，白色背景，细线条，低饱和配色，布局严谨，适合博士论文和双栏学术论文。

画布采用横向宽幅构图，比例约为 3.2:1，例如 3200 px × 1000 px。全图分成三个并排面板，面板之间留出清晰空隙。三个面板从左到右依次标注：
(a) 模块网络
(b) QPU 节点内部
(c) 远程资源调用链

每个面板使用极浅灰色或纯白背景，边框为浅灰细线，圆角较小。面板标签放在左上角。三个面板的高度一致，顶部对齐，底部对齐。全图阅读顺序从左到右推进，左侧回答网络边是什么，中间回答节点内部谁承担什么功能，右侧回答远程 Bell 对如何被上层协议调用。

面板 (a) 标题必须写：
(a) 模块网络

面板 (a) 画 5 到 7 个 QPU 节点组成的网络拓扑。QPU 节点画成浅蓝色圆形或小圆角方块，每个节点内部写：
QPU

节点之间用细蓝色连线连接，连线表示可宣告远程 Bell 对资源。网络形状应像中等规模的处理节点图谱，可以采用不规则网格或小型 mesh，而不是简单直线排布。连线旁边或面板底部放一个资源说明框，框内文字必须写：
可宣告 Bell 对
$F_{\mathrm{Bell}}, R_{\mathrm{link}}, \tau_h, m$

在网络中心附近放一行小字：
网络边 = 可调度纠缠资源

该面板的关键视觉洞见是：模块网络中的“边”不是普通导线，而是带有保真度、建立速率、宣告延迟和成功记录的远程纠缠资源。图中应让资源指标贴近边，而不是贴近节点。可以在一两条边上画小勾号或小圆点，表示 heralded success 事件。QPU 节点使用浅蓝填充，边使用蓝色细线，成功事件使用橙色小标记。

面板 (b) 标题必须写：
(b) QPU 节点内部

面板 (b) 放大单个中性原子 QPU 节点。用一个较大的白色圆角矩形表示 QPU 内部空间。内部放三类对象：绿色数据比特、红色通信比特、灰蓝色本地控制/调度单元。数据比特画成规则小圆点阵列，例如 5×3 或 6×3，放在 QPU 内部上半区。通信比特画成 1 到 2 个红色圆点，放在节点边缘或下半区，并用短线连接到光子接口小端口。控制/调度单元画成灰蓝色小圆角框，放在 QPU 内部下方或右下角。

数据比特旁边放一个说明框，文字必须写：
数据比特
本地门 / 存储 / 读出
综合征循环

通信比特旁边放一个说明框，文字必须写：
通信比特
原子--光子接口
远程纠缠收发

控制/调度单元内部文字必须写：
Pauli frame
路由选择
成功记录

通信比特到控制/调度单元之间应有短箭头，表示成功记录和路由信息进入控制层。数据比特到控制/调度单元之间也应有短箭头，表示本地纠错循环与 Pauli frame 更新。该面板的关键视觉洞见是：节点内部的数据比特承担本地计算和纠错循环，通信比特承担原子--光子接口和远程纠缠收发，控制单元把成功事件转换成可调度的上层资源。绿色用于数据比特，红色用于通信比特，灰蓝色用于控制与调度。

面板 (c) 标题必须写：
(c) 远程资源调用链

面板 (c) 画一条从左到右的资源调用链。使用 5 个圆角矩形依次排列，文字必须依次写：
可宣告 Bell 对
纯化
纠缠交换
门传送
跨模块门 / 逻辑操作

五个框之间用单向箭头连接，箭头方向从左到右。第一个框使用浅蓝色，表示远程 Bell 对；中间三个框使用浅灰或浅紫，表示资源处理流程；最后一个框使用浅绿色，表示进入计算或逻辑操作。调用链下方画一条细橙色时间线或事件线，在线上放 2 到 3 个小圆点，表示 heralded success 记录进入流程。

在事件线附近放一个说明框，文字必须写：
heralded success
成功记录 $m$

在调用链右下方放一行小字：
资源进入上层调度

该面板的关键视觉洞见是：远程 Bell 对先作为可宣告资源出现，随后可以经过纯化、纠缠交换和门传送，进入跨模块门或逻辑操作。成功记录 $m$ 应通过短箭头连接到调用链，表示它影响上层调度和 Pauli frame，而不是只作为实验日志存在。

整体色彩保持统一。QPU 节点和 Bell 对资源使用浅蓝色；数据比特使用绿色；通信比特和 heralded success 使用红色或橙红色；控制层使用灰蓝色；最终逻辑操作使用浅绿色。背景保持白色。线条使用深灰或低饱和蓝灰，线宽约 0.8 到 1.2 pt。文字使用清晰中文无衬线字体，保留 QPU、Bell、Pauli frame、heralded success、$F_{\mathrm{Bell}}$、$R_{\mathrm{link}}$、$\tau_h$、$m$ 等英文缩写和数学符号。

排版要求是信息密度中等偏高但保持清晰。每个面板只表达一个局部问题。面板 (a) 重点是网络边的资源指标；面板 (b) 重点是数据比特、通信比特和控制记录的角色分工；面板 (c) 重点是 Bell 对从物理资源进入协议调用链。三个面板之间可以用很淡的箭头或视觉方向感连接，也可以只靠从左到右的排布形成论证推进。所有框内文字应完整可读，避免文字与箭头重叠，避免资源指标被挤到边界。

最终图像应呈现正式学术论文中的解释性 schematic。画面应清楚说明模块化量子处理网络中“节点”“边”“接口”和“资源调用”四个对象的关系。图像可以作为后续 TikZ 重绘参考，因此几何关系、面板分割、标签位置和箭头语义都要明确。
