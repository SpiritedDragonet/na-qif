- **1.1 研究背景**（保持）

  - 1.1.4 **第一性原理端到端仿真需求**（原：第一性原理端到端仿真的必要性与本文创新；其中“创新”建议迁移到 1.2）

    

    

- 3.3 **光纤 BSM Heisenberg-POVM 统一建模**（原：光纤与 BSM 的 Heisenberg-POVM 统一建模）

  - 3.3.1 **Heisenberg-POVM 推回及成功判据**（原：Heisenberg-POVM 推回、成功判据与复杂度口径；把“复杂度口径”移到 3.4）

  4.1.1 **指标口径参数锚定**（原：指标口径、参数锚点与分层标定；把“分层标定”移到 4.2，避免重复）

- 4.2 **分层标定流程**（原：分层标定路径）

- 4.3 **结果图谱物理归因**（原：结果图谱与物理解释）

  - 4.3.1 结果图谱联合诊断链（保持；但把内部“证据链”拆成更清晰的段内层次）

- 4.4 **端到端性能误差预算**（原：误差预算与真假事件分解；同时把你目前放在 4.3.1 的“端到端可用纠缠质量”迁移到这里，形成“先诊断后汇总”的层次）

  

**结论**（conclusion.tex，保持环境不变）

- 建议内部结构固定为：研究问题回扣 → 主要结论（对应贡献点）→ 局限 → 展望（逐条对齐局限）

**附录A 补充数学推导**（appA.tex，保持）

- 建议明确在主文中“需要推导但不影响主线”的位置引用附录编号，避免附录成为孤岛。

------

## 分部分功能定位与具体优化方案

### 引言部分

#### 第1章整体功能

1. **把问题从“物理现象”提升到“可评估的工程任务”**：你做的是“端到端可用纠缠”的预测与优化，不是单个器件的理论推导。
2. **锁定读者的评判口径**：后文所有结果要落在一组统一指标上（你在第4章做得很好，但应该在绪论末就预告指标体系与分层对标策略）。
3. **给出清晰的贡献清单与边界清单**：盲审最怕“做了很多但不知道新在哪里、适用范围在哪里”。

#### 1.1 研究背景优化

- **功能**：建立“为什么需要端到端仿真”的必然性（而不是“可以做”）。
- **当前结构的潜在风险**：背景信息量很大，但“痛点—缺口—本文路线”的因果链容易被文献罗列稀释。
- **优化动作（不重写，只重排+加锚点句）**：
  - **把 1.1.1 的开头改成明确的任务定义句**：远程纠缠建立、Heralding、可用纠缠速率、条件态质量。然后再谈平台进展与网络化驱动。
  - **在 1.1.2 固化体系结构**：建议把你现有的链路示意图（fig:interface_arch）作为该小节“全章唯一结构图”，并在图注中明确：发射→QFC/滤波→光纤→BSM→点击记录→后验态。
  - **在 1.1.3 用一张“跨尺度噪声迁移”图作为叙事转折点**：你已有 fig:noise_vs_distance，很适合承担“为什么必须端到端”的论证职责。建议在正文紧随图后给出一句“结论性句子”：长距离端噪声主导导致“速率退化机制”和“保真度退化机制”不再同源，因此简化乘法预算失效。
  - **1.1.4 只讲“为什么现有方法不够”**：把“本文创新点”从背景末尾迁到 1.2，避免背景过早进入“自我评价”，提升学术叙事的客观性。

#### 1.2 研究目标及贡献优化

- **功能**：用审稿人的语言回答三件事：
  1. 你要预测什么（输入输出是什么）；
  2. 你怎么预测（核心技术路线是什么）；
  3. 你比已有工作多了什么（可检验的贡献是什么）。
- **建议写法（不新增子节，用 \paragraph 固定四段）**：
  - \paragraph{问题定义} 把“端到端链路”写成一个明确的数学对象：参数集合 (\theta) → 条件化测量事件集合 (\mathcal{E}) → 输出 ({p_s,p_t,p_f,F_t,S}) 与中间可观测量集合。这样第4章的指标定义就不显突兀。
  - \paragraph{研究目标} 用 2–3 条“可验证目标”表达：
    1. 在保持纯态 MPS 的条件下严格纳入损耗与探测统计；
    2. 给出可与实验逐项对齐的中间量输出；
    3. 输出可用于工程选点的 Pareto 前沿与部署判据。
  - \paragraph{主要贡献} 建议把贡献写成与你 conclusion.tex 三条结论一一对应的三条，并且每条都带“可复现产物”：模型、算法、指标体系、诊断链、误差预算。
  - \paragraph{论文边界} 把你结论里提到的简化假设提前“预告”一遍（例如单光子截断、桶式探测近似、PMD 频率相关性未显式建模等），审稿人会明显更愿意信任后文结果。

#### 1.3 论文结构安排优化

- **优化动作**：把现有三句结构安排改为“每章回答什么问题”的一句话版本，例如：
  - 第2章回答“为什么张量网络能算、怎么算、误差怎么控”；
  - 第3章回答“端到端链路如何门化并保持可收缩”；
  - 第4章回答“如何对齐实验并给出可操作的工程结论”。

------

### 主体部分

#### 第2章功能与优化

- **功能**：为审稿人建立“计算可信度”的三根支柱：
  1. 低秩截断有边界（误差可控）；
  2. TEBD 推进有标度（成本可解释）；
  3. 开放噪声与条件化测量有一致口径（统计不自相矛盾）。
- **建议优化点（基本不改结构）**：
  - 在 2.1 末尾加一段“与本文场景的对应关系”：(N) 不是自旋链长度，而是时间 bin 数；局域门来自系统–bin 相互作用；键维增长对应“时间相关性/记忆效应”而非空间纠缠。
  - 2.2/2.3 的小结段落里显式给出你后文会用到的**两条收敛检查**：
    - 截断误差（discarded weight）或 Schmidt 谱尾部；
    - Trotter 步长 (\Delta t) 的稳定性。
      这会直接为第4章“键维轨迹图”提供审稿友好的解释框架。
  - 2.4 把“对偶推回”的意义写得更“工程化”：你是在避免密度矩阵爆炸、把不可逆噪声统计编码进 effect，从而让“纯态 + effect”替代“混态演化”。

#### 第3章功能与优化

- **功能**：把真实链路拆成一套“可计算的门序列 + 可收缩的测量 effect”，并明确每个门对应的物理参数与实验可标定量。
- **最关键的结构优化（不加新节，只搬移内容）**：
  1. **给 3.1 和 3.3 各补一段“节引导段”**（可能已完成）：你现在这两节节标题下正文过短，读者会在视觉上感觉“节是空的”。解决方法不是加小节，而是在 \section 后加 5–8 行说明本节目标、输入输出、与后续小节关系。
  2. **把 3.3.1 标题中的“复杂度口径”迁移到 3.4**：
     - 3.3.1 专注于“effect 怎么构造、成功事件怎么定义”；
     - 3.4 专注于“复杂度瓶颈来自哪里、如何随窗口/枚举空间增长”。
       这样逻辑更线性，也更像 NeurIPS 风格的“方法 → 实现细节”分离。
  3. **在 3.4 增加一个“仿真流程图或伪代码”作为唯一新增图**（不新增章节）(可以参考项目代码详细完成)：
     - 输入：(\theta)、离散参数 (N,\Delta t)、窗口 (\Delta t_w)；
     - 输出：({p_s,p_t,p_f,F_t,S}) + 中间量（波包、HOM、模式分布）；
     - 主循环：发射 TEBD → QFC/滤波门 → 光纤信道推回 effect → BSM effect 收缩 → 统计。
       这张图对盲审极其重要，因为它把“故事”变成“流程”，能显著降低误解概率。

------

### 结果部分

#### 第4章整体功能

- **功能一：可核验**（能对齐实验中间量，不靠末端指标硬拟合）
- **功能二：可解释**（每一个退化都能归因到链路某一层：发射/传输干涉/探测条件化）
- **功能三：可决策**（输出 Pareto 前沿与部署判据，服务工程选点）

#### 4.1 实验对照口径参数锚定

- **功能**：先统一“评价尺度”，再谈“参数识别”。这一步决定你后文所有曲线是否可比。
- **优化动作**：
  - 把现 4.1.1 的内容明确分成两块（段内层次即可）：
    1. **指标定义**：({p_a,p_{11},p_s,p_t,p_f,p_{t|11},F_d,F_t,S}) 以及真假成功拆分的统计口径；
    2. **参数锚定**：哪些来自文献独立测量，哪些来自拟合（HOM/窗口/波包）。
  - 表 \ref{tab:param-anchor} 建议增加一列“角色”：Fixed / Fitted / Scanned（不增加新表，直接加列）。盲审老师看到这一列，信任度会显著上升。

#### 4.2 分层标定流程

- **功能**：把“逆问题”变成“若干个条件良好的子问题”，避免参数退化。
- **优化动作**：
  - 你已有标定顺序叙述（波包→直方图→窗口→HOM→BSM→末端），建议新增**一张流程图**（可与 3.4 的流程图风格一致，但内容不同）：
    - 每一步写清：输入观测量、拟合参数、输出约束（例如 (\tau_e)、(\Delta t_w)、(v_{\rm HOM})）。
  - 把式 \eqref{eq:calibration-objective} 的目标函数改成“分层目标函数”表述：每层只对应该层观测量，最后才汇总；避免审稿人误以为你在做一次性全参最小二乘。

#### 4.3 结果图谱物理归因

- **功能**：这里不是“展示很多图”，而是**建立可信的证据链**：源端时间结构 → 窗口筛选 → 两光子干涉 → 宣告模式 → 条件态。
- **结构优化（不加小节，只重排 4.3.1 内部）**：建议把 4.3.1 固定成三段 \paragraph（你现在已有两段 + 一段端到端，可做更清晰的归位）：
  1. \paragraph{源端时域结构数值收敛}（对应 fig_wavepacket + fig_bond_dim）
  2. \paragraph{窗口选择事件统计}（对应 fig_accept_hist + fig_window_tradeoff）
  3. \paragraph{干涉质量模式分解}（对应 fig_hom + fig_bsm_patterns）
     把“端到端可用纠缠质量”从这里移到 4.4（因为那是“汇总层”而非“诊断层”）。

#### 4.4 端到端性能误差预算

- **功能**：把最终结论放在“可用纠缠质量—可用速率—可部署成本”的统一坐标系里。
- **建议内部组织**（仍用 \paragraph，不新增小节）：
  1. \paragraph{条件态结构非定域性}（密度矩阵可视化 + CHSH）
  2. \paragraph{关键参数依赖}（距离、QFC 噪声、探测效率/背景二维平面）
  3. \paragraph{误差预算真假事件分解}（把你现 4.4 的文字放在这里，但需要补图，见下节图表建议）

#### 4.5 计算成本部署判据

- **功能**：回应审稿人最爱问的“能不能算、算到哪、瓶颈是什么、如何扩展”。
- **优化动作**：
  - 你现在用表 \ref{tab:timing-breakdown} 很好，但建议再补**一张复杂度缩放图**（不新增章节）：例如
    - (T_{\rm det}) 随“窗口 bin 数”或“枚举点击模式数量”增长的曲线（对数坐标）；
    - 或 (T_w) 随 (\chi_{\max}) / (N) 的增长曲线。
  - 部署判据那部分已经有“硬约束集合”，建议把阈值写成“论文中使用的默认阈值示例”（比如 (F_{\min})、(S_{\min})、(T_{\max}) 给一个你实验可用的量级），否则会显得形式化而不落地。

------

## 结果章节图表逐项审视与 NeurIPS 级画法建议

下面按你第4章现有 12 张图逐一给出：**保留/合并/替换建议**，以及“NeurIPS 风格怎么画更合适”。

### 总体原则（NeurIPS 口径）

1. **避免 3D**：除非 3D 提供不可替代的信息，否则会降低可读性并引发“视觉误导”的审稿直觉反感。
2. **一图一论点**：每张图只服务一个结论；若一图承载 3 个结论，审稿人会认为你在“堆图”。
3. **统一样式**：字号、线宽、坐标命名、单位、误差棒规则全篇一致；色彩不能成为唯一编码（黑白打印可读）。
4. **caption 自洽**：图注需要在不读正文的情况下说明：变量定义、扫描范围、固定参数（baseline）、关键结论一句话。

------

### 图1 fig_wavepacket（双臂波包时域结构）

- **保留**：是分层标定的第一根锚，也是后续窗口与 HOM 的前提。
- **优化**：建议与 fig_bond_dim **合并为同一张多面板图**（a,b），共享时间轴。
- **NeurIPS 画法**：
  - (a) 波包强度/概率密度 vs 时间：两臂曲线 + 标注主窗口 (\Delta t_w)（用半透明带或竖线）。
  - 若有实验数据：**点（实验）+ 线（仿真）**叠加，图注说明拟合出的时间常数。
  - 可加一个小 inset 放大尾部指数衰减区，避免主峰遮蔽尾部信息。

### 图2 fig_bond_dim（发射阶段 MPS 键维演化）

- **保留但降权**：它非常重要于“数值可信度”，但对“物理读者”是实现细节。
- **优化路径二选一**：
  - **方案A（更像 NeurIPS）**：并入“图1(b)”作为收敛证据；
  - **方案B（更像论文）**：正文保留但把主要讨论压缩到 3–5 行，把更细节（比如不同 (\chi_{\max}) 对比）放到附录。
- **建议补充**：仅给 (\chi(t)) 还不够“可核验”，建议加一个额外曲线/标注：**discarded weight** 或截断误差上界（哪怕是统计量），审稿人会更信。

### 图3 fig_accept_hist（点击时间分布）

- **保留**：窗口策略的直接依据。
- **你当前的“可再上一个台阶”点**：把直方图拆成**真成功密度**与**假触发密度**（暗计数/背景）两部分的叠加或并列。
- **NeurIPS 画法**：
  - 使用归一化密度（便于不同条件对比）；
  - 在图中标注：窗口内真成功占比 (p_t/p_s) 的数值或趋势；
  - 若只画总直方图，会被问：“你怎么证明窗口优化不是在吃噪声？”

### 图4 fig_window_tradeoff（窗口扫描下速率、保真度、假成功占比）

- **必须保留**：这是“工程决策图”。
- **当前风险**：如果你用多纵轴或把 3–4 个量塞在一张图里，审稿体验会很差。
- **NeurIPS 推荐布局**：做成**两面板或三面板**，共享 x 轴（窗口宽度）：
  - (a) (p_s) 或真成功率 (p_t)（建议同时给，或给 (p_t) 更硬核）；
  - (b) (F_t)；
  - (c) (p_f/p_s)（若版面紧张，(c) 可用右侧小面板或 inset）。
- **关键增强**：在图中用垂线标出你选定的 baseline 窗口（例如 70 ns），并在 caption 里写一句“该点满足……且位于……折中区”。

### 图5 fig_hom（HOM 曲线可见度）

- **必须保留**：它是不可分辨性的实证锚点，也是 BSM 可信度前提。
- **NeurIPS 画法**：
  - 数据点带误差棒（若实验给了统计误差），仿真用实线；
  - 图注写清“拟合模型”与“提取参数 (v_{\rm HOM})”的数值；
  - 若你有多个噪声条件，优先画“可见度 vs 噪声参数”的摘要图，把完整 HOM 曲线放补充材料/附录。

### 图6 fig_bsm_patterns（BSM 模式概率及真假分解）

- **强烈建议保留**：这是你“真假事件分解”最直观的证据。
- **优化**：建议使用**堆叠柱状图**：每个模式一个柱，柱内颜色/纹理区分真成功、暗计数、背景三部分。
- **NeurIPS 画法**：
  - 模式按“对目标态贡献大小”排序，而不是按编号；
  - 图注写清“成功判据”是哪些点击模式集合；
  - 与 HOM 图放在同一页（或同一大图的 (a)(b)），形成“干涉质量 → 模式分解”的证据链闭环。

### 图7 fig_bell_density_3d（三维柱状密度矩阵）

- **建议替换**（这是你当前最“NeurIPS 不友好”的图）：3D 柱状图阅读成本高、易遮挡、难比较。
- **最佳替代方案**（二选一，优先 A）：
  - **A 2D 热图**：分别画 (\Re(\rho))、(\Im(\rho)) 两张 2D heatmap（共享颜色条范围），并在格点上标注关键元素（比如 (\rho_{01,10})）。
  - **B Pauli 相关矩阵**：画 (T_{ij}=\mathrm{Tr}[\rho(\sigma_i\otimes\sigma_j)]) 的 3×3 相关矩阵热图，审稿人对“非定域性/CHSH”更直觉。
- **NeurIPS 额外加分**：画“(\rho-\rho_{\Phi})”的差分热图（仅显示偏离项），会比“看起来像 Bell”更硬核。

### 图8 fig_chsh（CHSH 参数趋势）

- **保留**：它给出“可用纠缠”的更强证据。
- **优化**：明确 x 轴扫描的是什么（距离/窗口/噪声/效率），并在 caption 中写明“测量基选择”或“采用的最优基/固定基”。
- **画法**：经典界 (S=2) 虚线保留；建议加一条你定义的“可用阈值”线（如果你在 4.5 里有 (S_{\min})）。

### 图9 fig_distance（成功率与保真度随距离）

- **必须保留**：它是“跨尺度迁移”的核心结果。
- **NeurIPS 画法建议**：
  - 分成两面板共享距离轴：上面板画 (p_t)（对数坐标），下面板画 (F_t) 或 (S)。
  - 不建议双 y 轴（审稿人普遍反感）。
  - 可用浅色背景标注“信号主导区/噪声主导区”的分界（你的绪论噪声迁移图能呼应这里，形成故事闭环）。

### 图10 fig_qfc_noise（QFC 背景噪声影响）

- **保留**：它回答“器件指标优先级”。
- **更强的表达方式**：把它从“一维扫描曲线”升级为“阈值图”或“等值线图”：
  - 例如画出满足 (F_t\ge F_{\min}) 的噪声上限随窗口/效率变化的边界线。
- **与 fig_detector_eff 的关系**：这两张图的信息高度互补，建议后续做成同一组图的不同面板（见下条）。

### 图11 fig_detector_eff（效率-背景二维热图）

- **必须保留**：二维工作平面是工程选型最有价值的输出。
- **NeurIPS 画法**：
  - 强烈建议**并列两张热图**：左为 (F_t)，右为 (p_t)（或单位时间真成功数），共享坐标轴与对齐的色条；
  - 在热图上叠加等高线：(F_t=0.5)、(S=2)、(p_f/p_s=) 某阈值等（任选 1–2 条，别太多）。
  - 如果你坚持保留“成功率切片”，建议作为右侧小面板 inset，而不是占据主版面。

### 图12 fig_rate_fidelity（速率-保真度前沿）

- **必须保留**：这是“部署判据”的图形化表达。
- **NeurIPS 画法**：
  - 用散点云表示所有扫描点，用“非支配前沿”高亮（粗线或突出标记）；
  - 用点的形状/透明度区分不同距离或不同窗口策略（避免仅靠颜色）；
  - 在图上标注 2–3 个代表性工作点（例如“高保真低速”“折中点”“高速度点”），并在 caption 写明这些点的参数组合。

------

## 第4章建议新增但目前缺失的关键图

这是“盲审老师最容易追问”的三类缺口，建议至少补其中两类（不增加章节，仅作为 4.2/4.4/4.5 的插图）：

1. **分层标定流程图**（放 4.2）
   - 这是把你“分层可观测量框架”讲成“可执行流程”的关键一图。
   - 画法：流程框 + 每步输入观测量 + 输出参数/约束 + 指向下一步。
2. **误差预算图**（放 4.4，强烈建议）
   - 你现在的误差预算是文字+公式，缺少“一眼看瓶颈”的图。
   - 推荐画法：
     - **瀑布图（waterfall）**：从 ideal 开始逐步打开噪声组（源端→QFC/滤波→光纤→探测），展示 (F_t) 或 (p_t) 的下降量；
     - 或 **堆叠条形图**：各噪声组对 (p_f) 的贡献占比（暗计数/背景/其他）。
   - 这张图极其“审稿友好”，因为它直接回答“该优化哪里”。
3. **数值收敛与缩放图**（放 4.5 或附录，正文给摘要版）
   - 至少给出一个：(F_t) 随 (\chi_{\max}) 或 (\Delta t) 的收敛曲线（1–2 条即可）。
   - 目的不是展示你算得多快，而是证明“结果不依赖数值选择”。

------

## 最后给你一个可直接执行的结构落地清单

1. 把第4章中“端到端可用纠缠质量”段落整体迁移到 4.4（先诊断后汇总）。
2. 4.1.1 只保留“指标定义 + 参数锚定”，把“分层标定”的叙述移到 4.2。
3. 用两张流程图把“仿真流程”和“标定流程”固定下来（分别放 3.4、4.2）。
4. 把密度矩阵 3D 柱状图替换为 2D heatmap 或 Pauli 相关矩阵图。
5. 将结果章节 12 张图压缩为“6–8 张主图 + 若干附录图”的叙事结构：主图讲结论，附录图讲验证/实现细节。
6. 给误差预算补一张瀑布图或堆叠条形图。
7. 所有主图 caption 补齐：扫描参数范围、固定基线参数、关键结论一句话。

